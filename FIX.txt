#TODO: 
# ERROR MESSAGE when empty file								#FIXED
# do main error civilised										#FIXED
# wrong arg type in call exp: int c(char a) <...> c(5) -> works, throws error only on identifiers, does not with LITS #FIXED
# if(5) works, in () should only be bool types			#FIXED
# c(5+d) d - bool , should print type error				#FIXED
# type printing													#FIXED
# Stringus dėk į patį programos galą ir išskirk kažkokia komanda
# Else if is just like simple if, make BR to the end of if block

5 task: 
# try pack unpack for casting float to int #DONE
# pasidaryk string_list index globalų, kad galėtum stdout'int normaliai

int a = 5; decl (out of func)
int a = 5; stmt decl (in fun)			
